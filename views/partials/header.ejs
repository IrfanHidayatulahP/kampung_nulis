<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>
        <%= title || 'Aplikasi' %>
    </title>

    <style>
        :root {
            --bg: #f6f8fb;
            --card: #ffffff;
            --muted: #6b7280;
            --accent: #2563eb;
            --danger: #ef4444;
            --radius: 10px;
        }

        body {
            font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
            background: var(--bg);
            margin: 0;
            color: #111
        }

        .container {
            max-width: 1000px;
            margin: 2.5rem auto;
            padding: 1rem
        }

        header.appbar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 12px 16px;
            background: linear-gradient(90deg, #fff 0%, #f8fafc 100%);
            box-shadow: 0 1px 4px rgba(12, 18, 38, 0.06);
            border-radius: 12px
        }

        .brand {
            display: flex;
            gap: 12px;
            align-items: center
        }

        .brand .logo {
            width: 42px;
            height: 42px;
            border-radius: 8px;
            background: var(--accent);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700
        }

        nav .navlinks a {
            margin-left: 12px;
            color: var(--muted);
            text-decoration: none;
            font-weight: 600
        }

        .card {
            background: var(--card);
            padding: 18px;
            border-radius: var(--radius);
            box-shadow: 0 6px 18px rgba(12, 18, 38, 0.06);
            margin-top: 1rem
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 12px;
            margin-top: 12px
        }

        .btn {
            display: inline-block;
            padding: 8px 12px;
            border-radius: 8px;
            background: var(--accent);
            color: #fff;
            text-decoration: none;
            font-weight: 600
        }

        .btn-ghost {
            background: transparent;
            border: 1px solid #e5e7eb;
            color: var(--muted)
        }

        .muted {
            color: var(--muted)
        }

        form input,
        form button,
        form textarea {
            font-family: inherit
        }

        .small {
            font-size: 0.9rem;
            color: var(--muted)
        }

        @media (max-width:700px) {
            .grid {
                grid-template-columns: 1fr
            }

            .container {
                padding: 0.5rem
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <header class="appbar">
            <div class="brand">
                <div class="logo">L</div>
                <div>
                    <div style="font-weight:700">
                        <%= title || 'Aplikasi' %>
                    </div>
                    <div class="small">Library / Sistem Peminjam</div>
                </div>
            </div>

            <nav class="navlinks">
                <% if (typeof user !=='undefined' && user) { %>
                    <span class="small muted">Hi, <strong>
                            <%= user.nama_lengkap || user.username %>
                        </strong></span>
                    <a href="/" class="small muted" style="margin-left:10px">Home</a>
                    <a href="/logout" class="small" style="margin-left:12px;color:var(--danger)">Logout</a>
                    <% } else { %>
                        <a href="/login" class="small muted">Login</a>
                        <% } %>
            </nav>
        </header>

        <!-- page body -->